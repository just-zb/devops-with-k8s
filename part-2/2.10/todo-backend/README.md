After configuring lok-stack and Grafana, select app=todo-backend in dashboard.
Run `run.sh` to start the backend container, and send two request via postman, one is ok, another one is too long.
![img.png](img/img.png)